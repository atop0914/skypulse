[project]
name = "skypulse"
version = "0.1.0"
description = "🌪️ SkyPulse - 智能天气机器人，基于 AI Agent 的天气查询应用"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.4",
    "uvicorn>=0.30.0",
    "langchain>=1.2.9",
    "langchain-openai>=1.1.7",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "httpx>=0.27.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "black>=24.0.0",
    "ruff>=0.4.0",
]

[project.scripts]
skypulse = "skypulse.main:main"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
# 开启哪些检查规则
# E: pycodestyle (基础风格)
# F: Pyflakes (错误检查)
# I: isort (导包排序)
select = ["E", "F", "I"]

[tool.ruff.format]
# 类似 gofmt 的设置
quote-style = "double" # 强制双引号，或者选 "single"
indent-style = "space"

[tool.black]
line-length = 100

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ruff>=0.15.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/skypulse"]
